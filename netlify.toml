[build]
  command = "npm run build"
  publish = "dist"

# React Router usa rotas no cliente — sem isso, atualizar a página em
# /raspadinhas (ou qualquer rota que não seja "/") retornaria 404.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
